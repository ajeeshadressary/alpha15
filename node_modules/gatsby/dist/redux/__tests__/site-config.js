"use strict";

var _config = require("../reducers/config");

var _config2 = _interopRequireDefault(_config);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe(`add site config`, function () {
  it(`allow you to add basic site config`, function () {
    var config = {
      siteMetadata: {
        title: "yo testing"
      },
      plugins: []
    };
    var state = (0, _config2.default)({}, {
      type: "SET_SITE_CONFIG",
      payload: config
    });
    expect(state).toMatchSnapshot();
  });

  it(`Validates configs with unsupported options`, function () {
    var config = {
      someRandomThing: "hi people",
      plugins: []
    };
    function runReducer() {
      return (0, _config2.default)({}, {
        type: "SET_SITE_CONFIG",
        payload: config
      });
    }
    expect(runReducer).toThrowErrorMatchingSnapshot();
  });
});